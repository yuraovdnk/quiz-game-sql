name: Test App
on: push
jobs:
  tests:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - name: output vars
        run: echo "tokennnnnnfn ${{vars.POSTGRES_PROD}}"
    
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install dependencies
        run: yarn

      - name: Test
        run: yarn test:e2e
        
      
