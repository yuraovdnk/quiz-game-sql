export enum SortedFieldsGame {
  published = 'published',
  createdAt = 'createdAt',
  body = 'body',
  updatedAt = 'updatedAt',
}

export enum GameStatus {
  PendingSecondPlayer = 'PendingSecondPlayer',
  Active = 'Active',
  Finished = 'Finished',
}

export enum AnswerStatus {
  Correct = 'Correct',
  Incorrect = 'Incorrect',
}
